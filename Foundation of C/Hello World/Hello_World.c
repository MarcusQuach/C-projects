#include <stdio.h>  // Include preprocessor aka turns 1's and 0's into text

int main() {    // Reads main function from top to bottom
    printf("Hello World\n"); // Printf is to format print 
    printf("This is my first code in C"); // Use new line to separate and print
    return 0;   // Return exit statement,
                // ; is basically a period, 
                // return 0 if script is successful
                // return 1 if not successful
}