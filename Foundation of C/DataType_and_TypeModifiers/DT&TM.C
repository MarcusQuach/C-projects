#include <stdio.h>

int main() {
        // unsigned int to add a int data type
        // to store values that arent negative
        unsigned int ui = -100;
        printf("ui: %u", ui);
        return 0;
}